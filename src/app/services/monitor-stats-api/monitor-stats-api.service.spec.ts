import { TestBed, inject } from '@angular/core/testing';

import { MonitorStatsApiService } from './monitor-stats-api.service';

describe('MonitorStatsApiService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MonitorStatsApiService]
    });
  });
});
