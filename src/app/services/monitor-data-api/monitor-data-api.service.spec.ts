import { TestBed, inject } from '@angular/core/testing';

import { MonitorDataApiService } from './monitor-data-api.service';

describe('MonitorDataApiService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MonitorDataApiService]
    });
  });
});
